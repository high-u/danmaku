# Danmaku アーキテクチャ仕様

## 開発ルール（絶対）

以下は本プロジェクトに関わる開発者・AI エージェントが**例外なく従う**ルールである。

1. **依存関係はコマンドでインストールし、必ず最新の安定版を使う**
   - パッケージマネージャ (`cargo add`, `npm install`, `swift package` 等) を介して追加する。`Cargo.toml` 等への手書き追記で版を決め打ちしない。
   - フィーチャフラグやバージョン指定も、コマンド出力（`cargo add` の表示するフィーチャ一覧、`cargo search`、公式ドキュメント等）で確認してから付ける。
   - 「以前見たバージョンだから」「あるだろうと推測」で版・フィーチャを書かない。

2. **バグ改修は最新の公式情報・ライブラリのソースコードを確認した上で行う**
   - 改修前に、関係する公式ドキュメント (freedesktop spec, GTK4 API リファレンス, クレートの docs.rs 等) または当該ライブラリのソースを確認する。
   - 「たぶんこう動く」「以前はこう書けた」で書き換えない。古い知識による推測修正は、別のバグを生むだけでありムダ。
   - 確認した根拠（URL / クレート名・バージョン・ファイル）をコミットメッセージや会話の中で示す。

これらは「望ましい」ではなく「必須」である。守らないことは設計品質と利用者の信頼を直接損なう。

## 概要

PC 画面のスクリーンショットを定期取得し、AI エージェントがそれを読み、画面内容に応じたコメント（やじ、アドバイス等）をニコ動風の弾幕として画面に流すシステム。

本リポジトリで提供する成果物は **スキル (SKILL.md) と単一責務のコマンド群**。スキルが AI エージェントに振る舞いを指示し、必要な道具としてコマンドを利用する。コマンド群は互いに独立しており、組み合わせを変えれば「弾幕」以外のユースケース（音声合成、マスコット表示、ログ書き込みなど）にも応用できる。

## 設計思想

このプロジェクトのアーキテクチャは **スキル中心・コマンド独立** という思想に立脚する。

### スキルが脳、コマンドは手足

- **スキル (SKILL.md)** が「やりたいこと」を AI エージェントに指示する。これがシステム全体の知識・意図の中枢。
- **コマンド** は単一責務の道具にすぎない。スキルがどのコマンドをどう組み合わせて使うかを決める。
- AI エージェントはシェル実行機能を使ってコマンドを叩く。コマンドは MCP ツール等として登録されるわけではなく、`ls` や `grep` と同列の通常の CLI プログラム。

### 何を変えれば挙動が変わるかが直感的

- 出力を弾幕 → 音声合成にしたい: SKILL.md を書き換え、`danmaku-cli` の代わりに `say` 等を叩かせる。GUI アプリ自体が不要になる。
- スクショの取り方を変えたい: `getscreens` の内部実装だけ差し替え。
- 観察観点を変えたい: SKILL.md のプロンプト指示だけ書き換え。

この**差し替え可能性**が、コマンドを統合せず独立に保つ理由。1 アプリに機能を詰め込めば失われる。

### インストールも AI エージェントが行う

ユーザーが手作業で構成要素を組み上げることは想定しない。スキルにインストール手順・設定手順を書き、AI エージェントがシェル経由で環境構築する。利用者は SKILL.md を信じて AI エージェントに任せれば動く状態にする。

### 定期実行は持たない。AI エージェントが自身でループする

cron / systemd timer などの OS 機構は使わない。利用者が AI エージェント CLI に対して「今から N 分間、スクリーンショット取得とコメント生成を繰り返して。danmaku スキルを使うこと」のような自然言語指示を出し、AI エージェント自身が SKILL.md に従ってループを回す。

この設計により:

- スケジューラ実装・設定が不要になる
- 「いつ・どれだけ・どんな観点で」を利用者が都度プロンプトで指定できる
- スクショ取得とコメント生成は同一エージェントの逐次実行なので、自然に直列化される（並列タイミングずれの問題は構造的に発生しない）

## 対応 OS

- Linux (X11)
- macOS

> Wayland は当面非対応。Windows は将来対応の可能性を残す。

弾幕 GUI 部分は OS ごとに別実装（透過オーバーレイのネイティブ実装が必要なため）。それ以外のコマンドはクロスプラットフォーム。

## AI エージェント要件

シェル実行機能を持つ AI エージェント CLI 全般に対応（Claude Code、Gemini CLI 等）。本プロジェクトは特定 AI エージェントに依存しない。利用者が cron などで起動するコマンドラインに具体的な CLI 名と引数を書く。

## LLM の前提

利用 LLM は **ローカル LLM を想定**（クラウド API は利用者判断）。スクリーンショットには機密情報が映る可能性があるため、第三者送信を避ける運用が標準。本プロジェクトはこの判断に関与しない。

## 全体構成

利用者の自然言語指示で AI エージェントが起動し、自分自身でループを回す。

```
[利用者プロンプト]
    例: 「今から 10 分間、スクリーンショット取得とコメント生成を
         繰り返して。danmaku スキルを使うこと」
    │
    ▼
[AI エージェント CLI]
    SKILL.md を読み込み、以下のループを実行:
    │
    │   ┌─────────────────────────────────────────────┐
    │   │ 1. シェルから `getscreens` を実行            │
    │   │    → JSON 配列でファイルパス等を受け取る     │
    │   │ 2. 受け取ったパスを AI エージェント標準の    │
    │   │    画像読み込み機能でロード                  │
    │   │ 3. コメント生成                              │
    │   │ 4. シェルから `danmaku-cli "..."` を実行     │
    │   │ 5. 必要に応じて待機して 1 に戻る             │
    │   └─────────────────────────────────────────────┘
    │
    ▼
[danmaku-cli] → Unix domain socket → [danmaku-gui]
    danmaku-gui が透過オーバーレイに弾幕表示
```

各構成要素は疎結合：

- `getscreens` はスクショを保存しパスを JSON で返すだけ。誰が読むかを関知しない。
- AI エージェント CLI は `danmaku-gui` を直接知らない。シェルで `danmaku-cli` を叩くだけ。
- `danmaku-cli` は socket に投げて即終了。
- `danmaku-gui` は socket からメッセージを受けて表示するだけ。誰が送ったかは関知しない。
- スキルを差し替えれば `danmaku-cli` を別コマンドに置換可能（音声合成、マスコット等）。

## アプリケーション / コマンド構成

### `danmaku-cli`

- 言語: Rust
- クロスプラットフォーム（Linux/macOS 共通ソース、OS 別ビルド）
- 役割: 引数を受け取り、Unix domain socket 経由で `danmaku-gui` に JSON で送信。即終了。
- 使用例: `danmaku --screen 0 "xxx" "yyy" "zzz"`
- フラグ案: `--screen`, `--color`, `--speed`, `--size`（設定ファイルの値を上書き）
- socket 接続失敗時: stderr にエラーを出し非ゼロ終了。AI エージェントの出力経由でエラーが LLM 側に返る

### `getscreens`

- 言語: Rust（ハイブリッド方式: モニター列挙・リサイズ・JSON 整形は Rust、画面取得は OS ネイティブツールに委譲）
- 内部実装:
  - Linux (X11): `maim` でフルキャプチャ → `image` クレート（純 Rust）でリサイズ → PNG 保存。モニター列挙は `xrandr` 出力を解析
  - macOS: `screencapture -x` でキャプチャ → `image` クレートでリサイズ。モニター列挙は `system_profiler` 解析（要検討）
- **出力: stdout に JSON 配列を 1 行で書き出し、即終了**
  ```json
  [{"screen": 0, "path": "/home/user/screens/0/20260516-120000.png", "timestamp": "20260516-120000"}]
  ```
- 既定動作: メインモニターのみ取得（配列要素 1 個）
- 拡張オプション案（**未実装、将来追加**）:
  - `--all` 全モニター取得（配列に複数要素）
  - `--screen N` 指定モニター取得
- 共通フラグ: `--dir <PATH>`（保存先）、`--size <PX>`（リサイズ後の長辺ピクセル）
- 画像サイズは利用者が `--size` で指定。アプリは妥当性に関与しない（文字の潰れ等は利用者の責任）
- ファイル名は時系列ソート可能な形式（例: `20260515-103045.png`）
- 失敗時は stderr にエラーメッセージを書き、非ゼロ終了。stdout に JSON を出さない
- スクショ取得そのものを目的とする独立コマンドであり、弾幕の存在を知らない。他のスキルやスクリプトからも単独で使える
- ImageMagick 依存は持たない（`image` クレートが純 Rust でリサイズを担う）

### `danmaku-gui-linux`

- 言語: Rust
- GUI: GTK4 (`gtk4-rs`)
- 役割: Unix domain socket を listen し、受信した文字列を透過クリックスルーオーバーレイに流す。**それだけ**。スケジューラもスクショ取得も AI エージェント起動も持たない
- トレイ: 後追い実装（AppIndicator / `ksni` クレート想定）
- 動作環境:
  - X11 のみ
  - GTK4 を提供する主要ディストリビューション（Ubuntu 22.04+ / Fedora 34+ / Debian 12+ / Arch 等）

### `danmaku-gui-macos`

- 言語: Swift（Xcode 不要、`swift` CLI でビルド）
- GUI: SwiftUI + `NSPanel`（`NSWindow.level = .screenSaver` でオーバーレイ）
- 役割: Linux 版と同等（socket 受信 + 弾幕表示のみ）
- 開発: macOS 実機上で行う

### 画像読み込み

- 専用ツールは作らない
- AI エージェント標準の画像読み込み機能 + SKILL.md の指示文で対応
- SKILL.md に書く想定の指示例:
  - 「`~/screens/N/` を作成日時降順で見て、最新および 1 つ前のスクショを読め」
  - 「直近の変化と最新画面に対し、◯◯な観点でコメントせよ」
  - 「必要ならさらに過去のスクショも遡って文脈を取れ」
- 「画像をプロンプトに直接添付」では LLM の自律的判断（過去を遡る等）が活かせないため、スキル化する意義がここにある

## プロセス間通信

### 方式

Unix domain socket（Linux/macOS 共通の POSIX 標準）

### Socket パス

`$XDG_RUNTIME_DIR/danmaku.sock`（Linux）
macOS は `$TMPDIR/danmaku.sock` 等（要検討）

### プロトコル

`danmaku-cli` → `danmaku-gui` への一方向、1接続 1メッセージ、JSON 1 行（改行区切り）。

例:

```json
{"screen": 0, "messages": ["こんにちは", "弾幕です"], "color": "white", "speed": 1.0}
```

- `screen`: 表示先ディスプレイ番号（必須）
- `messages`: 表示する文字列の配列（必須）
- `color`, `speed`, `size`: オプション（指定時は設定値を上書き）

GUI からの応答は当面なし（成功/失敗はソケット切断/エラーで判定）。

## 設定ファイル

### 場所

`~/.config/danmaku/config.toml`

### 想定項目

```toml
[display]
font_size = 32
font_family = "system"
color = "white"
opacity = 0.9
speed = 1.0
max_lines = 10

[socket]
path = ""        # 空なら OS 既定
```

> スクショ取得間隔・継続時間などは利用者がプロンプトで指定する（アプリ設定にしない）。SKILL.md は AI エージェントが解釈すべき指示の形式を定義する。

## 常駐プロセス（`danmaku-gui`）の方針

- ユーザーが意識できる形で常駐する（トレイアイコンで明示）
- 「裏で勝手に立ち上がる」挙動はしない
- **`danmaku-gui` はコマンドで手動起動できることを基本機能とする**。これがすべての応用の土台になる: OS の自動起動機構 (systemd user service / launchd) への組み込み、AI エージェントによるシェル経由起動、ユーザの動作検証のいずれもが、この「コマンドから起動可能」という基本機能の上に成立する。自動起動の組み込み是非は、まず手動起動で動作検証してから判断する。**この基本機能は将来も維持すること（削除・隠蔽しないこと）。**
- トレイアイコン実装は後追い

## Agent Skill

スキルがこのシステムの中枢である。コマンド群は手足にすぎない。

### Agent Skills 仕様準拠

本スキルは [Agent Skills 仕様](https://agentskills.io/specification) に従う。要点:

- スキルはディレクトリで、最低限 `SKILL.md` を含む
- `SKILL.md` は YAML フロントマター + Markdown 本文
- フロントマター必須: `name`（1-64 字 / `[a-z0-9-]` / ハイフン先頭末尾不可 / 連続ハイフン不可 / **親ディレクトリ名と一致必須**）、`description`（1-1024 字、「何をするか」と「いつ使うか」を含める）
- 任意: `license`, `compatibility`（500 字以内、環境要件）, `metadata`, `allowed-tools`（実験的）
- 本文は 500 行未満 / 5000 トークン未満を推奨。詳細は `references/` 配下の別ファイルに分割し相対パスで参照
- 任意ディレクトリ: `scripts/`（実行コード）、`references/`（追加ドキュメント）、`assets/`（静的リソース）
- progressive disclosure: 起動時にメタデータ (~100 tokens) のみ読まれ、スキル発動時に本文がロードされ、参照ファイルは必要時のみロードされる

### リポジトリ内配置

`gh skill install` の規約に従う:

```
skills/
└── danmaku/              # name フィールドと一致
    ├── SKILL.md
    └── references/       # 必要に応じて分割
```

### インストール

本プロジェクトの正規インストール方法は **`gh skill install <owner>/danmaku`**。利用者はこのコマンドでスキルをローカル環境に展開する。

```
gh skill install <owner>/danmaku
```

**この記述は本プロジェクトの方針として確定事項である。** Agent Skills 仕様自体は標準化された単一インストールコマンドを定義していないが、本プロジェクトは `gh skill install` を採用しており、将来のレビューでもこの記述を削除してはならない。

### SKILL.md の内容（方針）

スキルには以下を含める。利用者がスキルをインストールし、AI エージェントに「環境構築して」と頼めば必要な依存とアプリのビルドが整い、「今から N 分間…」のような指示でループが回る状態にする。

- フロントマター: `name: danmaku`, `description`（弾幕生成の用途と、いつスキルを使うべきかを具体的なキーワード込みで記述）, `compatibility`（X11, GTK4, maim, xrandr 等）
- 弾幕の用途と前提（ローカル LLM、X11、シェル実行可能な AI エージェント）
- `danmaku-cli` / `getscreens` の使い方（シェルコマンド例、JSON 出力の読み方）
- `danmaku-gui` のインストール手順（OS 別、ビルド or バイナリ取得）と**手動起動手順の案内**
- 設定ファイル (`~/.config/danmaku/config.toml`) の生成手順
- AI エージェントへの自律的振る舞いの指示:
  - 利用者の自然言語指示 (例: 「10 分間繰り返して」) をどう解釈してループするか
  - 1 ターンの流れ: `getscreens` 実行 → JSON のパスを画像読み込み機能でロード → コメント生成 → `danmaku-cli` 実行
  - 何を観察するか、どうコメントするか（過去スクショを参照する場合の指示含む）
  - 終了条件の判断

### 出力先の差し替え

SKILL.md で「画面に弾幕で流せ」を「`say` コマンドで読み上げよ」「マスコットアプリ XXX に投げよ」と書き換えれば、出力先が変わる。`danmaku-gui` も `danmaku-cli` も不要になり、構成要素を取り外して別の道具を組み合わせるだけで別ユースケースが成立する。これがスキル中心設計の利点。

## モノレポ構成

```
danmaku/
├── apps/
│   ├── danmaku-cli/        # Rust (Linux/macOS 共通ソース)
│   ├── danmaku-gui-linux/  # Rust + GTK4
│   ├── danmaku-gui-macos/  # Swift
│   └── getscreens/         # Rust (maim/screencapture を内部で呼ぶハイブリッド)
├── skills/
│   └── danmaku/
│       └── SKILL.md
├── SPECS.md
└── NOTE.md
```

## 開発順序

1. `danmaku-gui-linux` の最小プロトタイプ（X11 透過オーバーレイに文字 1 行流す）— ここで透過クリックスルー実現可否を実証
2. `danmaku-cli` + socket 通信
3. 設定ファイル読み込み
4. `getscreens` (Rust + maim ハイブリッド、メインモニター JSON 配列出力)
5. `skills/danmaku/SKILL.md`（Agent Skills 仕様準拠、インストール手順とループ指示）
6. トレイアイコン実装
7. `danmaku-gui-macos`（macOS 実機で）

## 未決事項

- macOS の socket パス規約
- macOS でのモニター列挙手段（`system_profiler` 解析か別手段か）
- `getscreens` のクリーンナップ戦略（日付ローテーション等）
- トレイメニュー項目
- ログ・履歴機能の要否

## 実装ノート (アプリ別)

- danmaku-gui-linux: [apps/danmaku-gui-linux/IMPLEMENTATION.md](apps/danmaku-gui-linux/IMPLEMENTATION.md) — X11 オーバーレイの実装根拠と分類

