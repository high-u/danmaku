# Danmaku

## 概要

ニコ動の弾幕と呼ばれる画面上を文字列が流れるものを作る。
流す文字列は、PC 画面を LLM が監視し、画面の表示に対して、やじ飛ばしたり、アドバイスしたり、する。この辺はプロンプトで自由に設定。
専用アプリを作るのではなく、　https://agentskills.io のスキルとして作成する。
スキルで利用する次の CLI を作成する想定。

- 弾幕 CLI
    - `danmaku "xxxx" "yyyy" "zzzz"` の様に投げると、 xxxx、yyyy、zzzz が画面を流れる。
    - 呼び出しは CLI が良いが、アプリとしては GUI アプリになるのかな？
    - LLM がツールとして呼び出す想定。
    - フォントサイズやカラー、流れる速度などの設定を行う GUI もしくは設定ファイルが必要。
- スクリーンショット CLI
    - サブディスプレイ含め指定のフォルダに保存する。
    - 画像の縦ピクセル指定し、アスペクト比を保持したまま縮小する。
    - `getscreens --dir "~/screens" --size 720`
    - 取得するディスプレイを指定する設定ファイルがあっても良いかも。もしくは、コマンドのオプションに用意。
    - cron などの、定期実行プロセスが呼び出す想定。 30 秒おき、1 分おき、とか。
- 画像読み込みツール
    - これはどうする迷いがある。
    - 最近の AI エージェントは、画像読み込みツールを持っている。
    - 読み込みたい画像は、最新のスクリーンショットと、ひとつ前のスクリーンショット。変化があるか無いかも見たいから。
    - スキルで、「フォルダと、作成日時が新しいものから 2 つを読み込んで、最新のスクリーンショットに〇〇な観点でコメントして。ひとつ前のスクリーンショットからの変化もコメント対象にして。」、のような指示で、出来そうな気もする。
    - もし前述の方法が難しい可能性があるなら、画像取得の MCP サーバーを作成する。要相談。
- Agent Skills の弾幕スキル
    - SKILL.md の作成
    - 前述の CLI がインストールされていない場合のインストール方法も明記し、AI エージェントが環境構築できるようにする。

## 対応 OS

- linux
- macos
